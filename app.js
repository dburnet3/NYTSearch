// things we need to grab from API: article title; author

var articles = [];

var articleSearch = $("#XXXXX").val().trim();

// The article from the textbox is then added to our array
articles.push(articleSearch);